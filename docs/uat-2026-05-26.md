# Roentgen UAT Gate - 2026-05-26

Purpose: keep the 2026-06-18 demo decision evidence-based. This is not a final clinical UAT. It records what was actually verified after PR #49 and what remains unverified.

## BLUF

Current verdict: limited demo only. Build, unit coverage, and renderer empty-state smoke passed locally, but real DICOM hands-on UAT was not executed in this run.

## True-Purpose Check

Roentgen exists so Kyokucho can inspect his own horse DICOMs in a commercial-quality app, understandable to a non-veterinarian owner, before the 2026-06-18 demo. This gate treats PR/merge as evidence only; it does not count as success unless the app can be safely shown without misleading measurements, accidental data loss, or silent failure.

## Evidence Collected

| Command | Result | Notes |
|---|---|---|
| `pnpm run build` | PASS | Production renderer and Electron bundles built. Vite emitted existing deprecation/chunk-size warnings only. |
| `pnpm test` | PASS | 41 test files, 454 tests passed. The expected ErrorBoundary test logs `viewer failed` while asserting recovery behavior. |
| `pnpm run lint` | PASS | Biome check passed. Existing schema version info: config schema points to 2.4.10 while CLI is 2.4.13. |
| `pnpm exec playwright test --project=renderer` | FAIL | First attempt failed with EPERM writing `test-results/.last-run.json`. |
| `pnpm exec playwright test --project=renderer --output=/private/tmp/roentgen-pw-results` | FAIL | Playwright could write artifacts, but Chromium was missing at `/Users/kouiso/Library/Caches/ms-playwright/chromium_headless_shell-1217/...`; no browser smoke assertion executed. |
| `pnpm exec playwright install chromium` | PASS | Installed Playwright Chromium and headless shell v1217 locally. |
| `pnpm exec playwright test --project=renderer --output=/private/tmp/roentgen-pw-results` | PASS | 6 renderer empty-state smoke tests passed on 2026-05-27. |
| `pnpm exec playwright test --project=electron e2e/electron/baseline.spec.ts --output=/private/tmp/roentgen-electron-baseline-results` | PASS | Electron autoloaded fixture DICOM, captured `test-results/baseline-autoload.png`, and verified WW/WC keyboard and mouse operations changed the rendered canvas. |
| `pnpm exec playwright test --project=electron e2e/electron/freehand-annotation.spec.ts e2e/electron/clear-reload.spec.ts --output=/private/tmp/roentgen-electron-annotation-clear-results` | FAIL then FIXED | Freehand annotation passed. Clear reload initially timed out because the E2E still clicked the old `クリア` accessible name after the UI moved to confirmed `全クリア`. |
| `pnpm exec playwright test --project=electron e2e/electron/clear-reload.spec.ts --output=/private/tmp/roentgen-electron-clear-results` | PASS | Updated the E2E to accept the confirmation dialog and click `全クリア`; clear + fixture reload stayed alive. |
| `pnpm exec playwright test --project=electron --output=/private/tmp/roentgen-electron-all-results` | PASS | 3 Electron smoke tests passed: baseline DICOM/WW-WC, clear reload, and freehand annotation. |
| `pnpm exec playwright test e2e/electron/` (all 4 specs) | PASS | All 4 Electron specs pass after 7600036 ToolPanel redesign fixes (commit afd434a, 2026-05-30). Measurement overlay freshly created distance+angle in real Electron app. |
| `pnpm exec playwright test e2e/electron/` (all 5 specs) | PASS | All 5 Electron specs pass after adding corrupt-file spec (2026-05-30). Non-DICOM error surfacing verified end-to-end. |

PR evidence:
- PR #49: https://github.com/kouiso/roentgen/pull/49
- Merge commit: `ddaa4f47904b68636721ffaddd5db9c2be8abd05`
- Review fix: Gemini Code Assist noted `window.confirm` spy cleanup; fixed in `5e6a3a2`.
- GitHub Actions: red because jobs did not start due billing/spending-limit annotation, not because command output failed.

## UAT Scenario Matrix

| Scenario | Expected | Actual 2026-05-26 | Verdict |
|---|---|---|---|
| Load DICOM | User can load a DICOM and see image content. | Electron baseline autoloaded `public/test.dcm` and captured `test-results/baseline-autoload.png`. Real owner DICOM hands-on is still not executed. | Partially verified |
| Equine WW/WC | User can switch to horse-relevant presets and understand current viewing state. | Electron baseline verified keyboard preset and mouse WW/WC drag changed rendered canvas average channel. | Verified locally |
| Measurement | Measurements must not imply calibrated mm when uncalibrated, and destructive measurement deletion must be recoverable/confirmed. | Electron measurement spec freshly created distance and angle overlays, verified SVG evidence (1 line+2 circles, 2 lines+3 circles). Screenshots written to `test-results/`. | Verified locally |
| Annotation/record | User can annotate without accidental loss. | Electron freehand annotation smoke created and rendered a polyline; screenshot and JSON evidence were written under `test-results/`. | Verified locally |
| Corrupt/non-DICOM | Bad files should surface understandable errors without crashing. | Electron corrupt-file spec launched app with `public/corrupt-fixture/not-a-dicom.dcm`, verified header showed "エラー: レントゲン画像ではありません", no crash. Screenshot in `test-results/corrupt-file-error-state.png`. | Verified locally |
| All clear | Header all-clear and tool-panel all-clear must not discard all DICOMs without confirmation. | PR #49 added header confirmation and tests. Electron clear reload now accepts the confirmation dialog, clears to idle, reloads fixtures, and repeats without renderer death. | Verified locally |

## Known Blockers Before "Show Freely"

1. Run real owner-DICOM hands-on UAT with Kyokucho or a trusted reviewer and record whether the app is "show", "limited show", or "do not show".
2. ~~Add or run a measurement-focused Electron smoke that freshly creates distance/angle measurements and captures the resulting overlay.~~ Done — afd434a (2026-05-30).
3. ~~Add Electron e2e for corrupt/non-DICOM error surfacing.~~ Done — corrupt-file.spec.ts (2026-05-30, issue #78).
4. Fix GitHub Actions billing/spending-limit blocker and rerun PR/main CI.

## 6/18 Decision State

As of 2026-05-30 (wave7): all 5 Electron smoke specs pass (baseline, clear-reload, freehand-annotation, measurement, corrupt-file). All UAT scenarios now "Verified locally" except "Load DICOM" (real owner DICOM pending). Remaining blocker: real owner-DICOM hands-on UAT and GitHub Actions billing fix.

## Rollback / Recovery

PR #49 is small and revertable. If header clear confirmation causes a demo issue, revert merge commit `ddaa4f47904b68636721ffaddd5db9c2be8abd05` or restore the previous `clearFiles` handler, but that reopens the accidental all-clear risk.
